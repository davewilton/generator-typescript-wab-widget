var registerSuite: any = intern.getInterface("object").registerSuite;
var assert: Chai.Assert = intern.getPlugin("chai").assert;

import WidgetUnderTest = require("../../<%= subWidgetName %>");

registerSuite("<%= subWidgetName %>", () => {

    var widget: WidgetUnderTest = null;

    return {
        name: "<%= subWidgetName %>",

        beforeEach(): void {
            // actions before each test
        },

        tests: {
            // very simple test
            "Create a <%= subWidgetName %> widget"(): void {
                let create: () => void = () => {
                    let a: any = new WidgetUnderTest({}, "node");
                };
                assert.doesNotThrow(create);
            },
        }

    };
});